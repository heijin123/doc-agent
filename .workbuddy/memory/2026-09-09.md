# 2026-09-09

## 代码演进（异步摄取 + 并发加固）
- 切片质量缓存：config.CHUNK_CACHE_DIR + pipeline._dump_chunks_cache 落盘 data/chunk/{stem}.md
- 异步摄取：server.py 加 ThreadPoolExecutor + _INGEST_JOBS 注册表 + POST 落盘即返 job_id/eta + GET /status + GET /jobs；ETA = pymupdf 页数 × 2s
- Chroma 并发：store.py get_collection 单例 client + _store_lock 串行化；4 并发各 120 块零错误
- 模型切 qwen3.7-text-embedding-flash（用户 .env，1024 维），充值后可用（初判欠费误报已更正）
- upload.html：异步契约（XHR 拿 job_id 翻处理中 + 不确定动画）；「最近上传记录」面板 HTML 就位，轮询回填 JS 待接

## README 补全（用户 review 前收尾）
- 补 §2 特性(切片缓存/异步摄取/并发安全)、§3 Embedding 模型可切换注释、§4.5 异步摄取架构、§5.5 API 表 + 异步 curl 示例、§6 server.py 注释 + data/chunk、§7.7 切片缓存 §7.8 异步落库、§9 roadmap、§12 已知局限
- 如实标注 upload.html jobs 面板 JS 待接（今天 review 重点待补项）

## 用户 review 待补
- 中金跨页表格 / 章节标题被吞的 PDF 语义切块优化

## 前端体验修复（16:44 后，用户反馈「标黄不标绿 + 刷新不显示最近记录」）
- 根因：upload_one_entry 拿到 job_id 后设 status=processing（黄）并调用不存在的 poll_job → 一直卡黄；「最近上传记录」面板 JS 整段未写（无拉取/轮询/刷新绑定）
- 修复 upload.html：
  - 上传成功即 status=ok（绿「上传成功」，元信息「后台处理中」），不再标黄卡死；已上传项保留在列表供确认，不再自动移除
  - 新增 recent 区块：load_recent_jobs() 拉 /api/v1/ingest/jobs 渲染面板；schedule_recent_poll() 每 3s 轮询仅当存在非终态任务；refresh_recent_button 绑定；页面加载即拉一次（刷新可回看）
  - 状态映射修正：服务端完成态为 status="ok"（非 "done"），badge/标签/结果解析统一按 "ok"；补 CSS .status_badge.queued
  - 修回此前误删的 clear_all_button 监听器；node --check 语法通过
- 服务实测：8001 在跑，/jobs 返回 {jobs:[{status:"ok",result:{chunks_created...}}]}，前端可正常渲染（内存已存在一条电子城任务）
- README §4.5/§9/§12 去掉「jobs 面板 JS 待补」过时描述，改为已联调
- 注意：同文件多次并行 Edit 仍会丢写回（已复发），关键改动务必串行 + node 语法校验兜底
