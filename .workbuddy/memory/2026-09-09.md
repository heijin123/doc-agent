# 2026-09-09

## 代码演进（异步摄取 + 并发加固）
- 切片质量缓存：config.CHUNK_CACHE_DIR + pipeline._dump_chunks_cache 落盘 data/chunk/{stem}.md
- 异步摄取：server.py 加 ThreadPoolExecutor + _INGEST_JOBS 注册表 + POST 落盘即返 job_id/eta + GET /status + GET /jobs；ETA = pymupdf 页数 × 2s
- Chroma 并发：store.py get_collection 单例 client + _store_lock 串行化；4 并发各 120 块零错误
- 模型切 qwen3.7-text-embedding-flash（用户 .env，1024 维），充值后可用（初判欠费误报已更正）
- upload.html：异步契约（XHR 拿 job_id 翻处理中 + 不确定动画）；「最近上传记录」面板 HTML 就位，轮询回填 JS 待接

## README 补全（用户 review 前收尾）
- 补 §2 特性(切片缓存/异步摄取/并发安全)、§3 Embedding 模型可切换注释、§4.5 异步摄取架构、§5.5 API 表 + 异步 curl 示例、§6 server.py 注释 + data/chunk、§7.7 切片缓存 §7.8 异步落库、§9 roadmap、§12 已知局限
- 如实标注 upload.html jobs 面板 JS 待接（今天 review 重点待补项）

## 用户 review 待补
- upload.html 最近上传记录面板轮询 JS（add_recent_job / poll_recent_job / refresh_recent_button 绑定）
- 中金跨页表格 / 章节标题被吞的 PDF 语义切块优化
